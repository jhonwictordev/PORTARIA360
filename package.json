{
  "name": "condominium-access-saas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MVP SaaS API-first para controle de acesso em condominios residenciais e comerciais.",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "db:migrate": "node scripts/migrate.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
